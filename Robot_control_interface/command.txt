####################################################################################################
# PRE-STEP1: Stereo Calibration
# run the 'python3 calibration_stereo_camera_node_1080p.py' for providing L-R images for stereo calibration
# open one terminal and run 'source /opt/ros/melodic/setup.bash'
# then run 'clion'
# then load the CMakelist.txt or reload it
# After a while, we can see the button become green, then we can press it to run the code
# Press the BLANK button in keyboard to collect one sample
# Repeat for about 15 times (NOTE: KEEP　ALL CHECKBOARD ARE IN BOTH LR IMAGES !!!)
# Press the Button 'A' to start calibration
# Then we can see the parameters in terminal or in *.YML file
# Replace the parameters in yolo_track_five_tools_merge_advanced.py line 102~115

# PRE-STEP2: Hand-eye Calibration
# Run code 'python hand_eye_calibration.py'
# Press 's' to save images, ~20 images is enough, delete similar close images; Press 'q' to exit.
# Do hand-eye calibration and replace the eTc parameter in /robots.ur5.py line37 and self.Kc in Mainwindow_ROS_MultiTools.py line91.

# After all the setup, use the Joystick to test whether it generates right action "left-right-zoom in/out ..."

####################################################################################################
# STEP1: Initial the ros related settings
source /opt/ros/melodic/setup.bash
roscore

# STEP2: Publish the 1080p images
# SET THE ZOOM LEVEL TO 0 !!!!!!!!!!!!!!!!!!! (default is 1...)
# NOTE: R > L black region area
# NEED to plug out-and-in the box card for initialization
# Then run the following code
v4l2-ctl -d /dev/video0 -i 1
# NEED TO CONNECT THE NETWORK FOR UTERUS MANIPULATOR
python3 stero_camera_node_1080p.py
# If No images are shown, use the command in terminal <ps -aux> and then kill the corresponding PID process <kill -9 PID>
# Then run the <python3 stero_camera_node_1080p.py> again.

# STEP3: Run the detection program
python yolo_track_five_tools_merge.py --weights ../yolov5/runs/train/exp13/weights/best.pt --conf 0.25 --view-img --classes 1 3 5 7 9


# STEP4: RUN the main program

python3 main.py
rosrun joy joy_node
roslaunch ur_modern_driver ur5_bringup.launch robot_ip:=192.168.3.33
rostopic echo joint_states
rostopic echo joy
